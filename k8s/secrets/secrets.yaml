# IMPORTANT: Replace all values below with base64-encoded real values.
# Generate with: echo -n 'value' | base64
#
# This file is a TEMPLATE. Do not commit real secrets to source control.
# In production, use a secrets manager (Vault, Azure Key Vault, Sealed Secrets).

apiVersion: v1
kind: Secret
metadata:
  name: ztp-secrets
  namespace: ztp
type: Opaque
data:
  # echo -n 'changeme' | base64
  POSTGRES_PASSWORD:   Y2hhbmdlbWU=
  JWT_SECRET:          Y2hhbmdlLXRoaXMtc2VjcmV0
  OIDC_CLIENT_SECRET:  ""
