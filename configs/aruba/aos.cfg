hostname {{ hostname }}

! --- Management VLAN1 with DHCP ---
vlan 1
   name DEFAULT_VLAN
   ip address dhcp-bootp

! --- Local user ---
password manager user-name admin plaintext {{ local_password }}

! --- SSH (no cipher/MAC tuning available on ProVision) ---
crypto key generate ssh rsa
ip ssh version 2
no telnet-server

! --- SNMP v3 ---
! Note: some older ProVision firmware only supports SNMPv3 noAuthNoPriv or authNoPriv.
! Verify your firmware supports authPriv before deploying.
snmpv3 user snmp-admin auth sha {{ snmp_auth_pass }} priv aes {{ snmp_priv_pass }}
snmpv3 group SNMP-RO user snmp-admin sec-model ver3
snmpv3 access SNMP-RO match-prefix "" sec-model ver3 sec-level authpriv read-view "all" write-view "" notify-view ""
snmp-server location {{ location }}
snmp-server contact noc@{{ domain }}

banner motd {{ location }}
*************************************************************
* AUTHORIZED ACCESS ONLY                                    *
* Unauthorized access is prohibited and will be prosecuted.*
* All sessions are monitored and logged.                    *
*************************************************************
@